# ============================================================
# CyberPs — Investigation Hunter Frontend (Vite)
# Copy this file to .env and set your backend URL.
# NEVER commit .env to version control.
# ============================================================

# Backend API base URL (no trailing slash)
VITE_API_URL=http://localhost:5000/api
